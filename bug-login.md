# ログイン画面の中央配置（システム名・ユーザー・パスワード）

## 要望
- ログイン画面で、**システム名 / ユーザー名入力 / パスワード入力 / ログインボタン**を「画面の真ん中」に配置したい。

## 目標（完了条件）
- 画面ロード直後、ログインカード（またはフォームのブロック）が **上下左右の中央付近**に配置される
  - 例: 画面高の中央を基準に、フォーム全体が視認しやすい位置に来る
- システム名（例: `Shipping Service Admin`）はフォーム上部で **中央揃え**
- 入力欄（ユーザー/パスワード）もカード内で **自然に中央寄せ**（カード自体が中央にあることが前提）
- 画面幅が狭い場合（モバイル）でも、左右に余白を持たせつつ **1カラムで崩れない**

## 前提（設計との整合）
- `docs/design/SCR-010-login.md` では「画面中央にログインカードを配置」「最大幅 400px」想定。
- したがって本件は **SCR-010 の視覚要件に寄せる**対応が妥当。

## 対応案（ソース修正方針のメモ）
### 1) 画面全体（外側）を中央寄せ
- 外側コンテナを以下の考え方で統一する
  - `min-height: 100vh`
  - `display: flex`
  - `align-items: center`
  - `justify-content: center`
  - `padding: 16px`（極小画面で端にくっつくのを防ぐ）

### 2) ログインカード（内側）の幅を固定しすぎず、最大幅を設ける
- `width: 100%`
- `max-width: 400px`（SCR-010 準拠）

### 3) カード内のヘッダーを中央揃えに統一
- システム名（例: `Shipping Service Admin`）は `text-align: center`
- アイコン/ロゴがある場合も中央揃え（上→下の縦並びが基本）
- 「新規登録リンク」等の補助導線がある場合：
  - ヘッダー行で左右に散らす（`space-between`）のではなく、
  - ボタンの下などに小さく中央配置（または右寄せでもよいが、まずは中央寄せ優先）

## 現状確認ポイント（どこでズレるか）
以下のどれかで「真ん中」から外れがちです。
- 外側レイアウトが `justify-content: flex-end` / 上部に寄るマージンが入っている
- 内側ヘッダーが `space-between` になっており、タイトルが左に寄って見える
- `mt-*`（上マージン）固定値で、画面高により位置が変動する

## 参考（現状の実装例）
- Next Shadcn は外側で中央寄せ済み
  - `apps/admin-next-shadcn/src/app/(auth)/login/page.tsx`
- MUI は外側カードは中央寄せだが、ヘッダーが左右に散って「中央に見えない」可能性がある
  - `apps/admin-mui/src/pages/auth/Login.jsx`
  - `apps/admin-mui/src/sections/auth/AuthWrapper.jsx`
- Horizon UI は `mt-*` で上寄りになりやすい（見た目が中央にならない可能性）
  - `apps/admin-horizon-ui/components/auth/AuthUI.tsx`
  - `apps/admin-horizon-ui/components/auth/index.tsx`

## 動作確認チェック
- 代表解像度で目視
  - 1366x768 / 1440x900 / 1920x1080
  - iPhone 相当（幅 375 前後）
- ブラウザズーム 90% / 110% でも中央配置が破綻しない
- 入力フォーカス（Tab移動）してもレイアウトが跳ねない

